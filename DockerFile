# 使用 OpenJDK 17 作为基础镜像
FROM eclipse-temurin:17-jdk-jammy

# 设置工作目录
WORKDIR /app

# 复制构建好的 JAR 文件（假设打包后是 target/wechat-ai-demo-0.0.1.jar）
COPY target/wechat-ai-demo-0.0.1.jar app.jar

# 暴露端口（微信回调通常用 80/443）
EXPOSE 80

# 启动 Spring Boot 应用
ENTRYPOINT ["java", "-jar", "app.jar"]
